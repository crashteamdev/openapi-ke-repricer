content:
  application/json:
    schema:
      required:
        - strategy
        - keAccountShopItemId
      properties:
        keAccountShopItemId:
          type: string
          format: uuid
        strategy:
          $ref: ../schemas/Strategy.yaml
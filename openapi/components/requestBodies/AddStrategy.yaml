content:
  application/json:
    schema:
      required:
        - strategyType
        - strategyParams
        - keAccountShopItemId
      properties:
        keAccountShopItemId:
          type: string
          format: uuid
        strategyType:
          $ref: ../schemas/StrategyType.yaml
        strategyParams:
          $ref: ../schemas/StrategyParams.yaml
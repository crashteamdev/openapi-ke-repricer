content:
  application/json:
    schema:
      required:
        - strategyParams
        - keAccountShopItemId
      properties:
        keAccountShopItemId:
          type: string
          format: uuid
        strategyParams:
          $ref: ../schemas/StrategyParams.yaml
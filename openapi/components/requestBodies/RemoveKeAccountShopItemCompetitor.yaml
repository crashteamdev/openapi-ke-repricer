content:
  application/json:
    schema:
      required:
        - shopId
        - shopItemId
      properties:
        shopItemRef:
          $ref: ../schemas/ShopItemRef.yaml
        competitorProductId:
          type: integer
          format: int64
        competitorSkuId:
          type: integer
          format: int64

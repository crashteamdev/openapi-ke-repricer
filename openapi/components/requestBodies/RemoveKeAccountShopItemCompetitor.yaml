content:
  application/json:
    schema:
      required:
        - shopId
        - shopItemId
      properties:
        shopItemRef:
          $ref: ../schemas/ShopItemRef.yaml
        competitorProductId:
          type: number
          format: int64
        competitorSkuId:
          type: number
          format: int64
